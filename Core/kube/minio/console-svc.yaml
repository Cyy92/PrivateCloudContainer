apiVersion: v1
kind: Service
metadata:
  name: console-svc
  namespace: minio-operator
  labels:
    app: console
spec:
  type: NodePort
  ports:
    - port: 9090
      protocol: TCP
      targetPort: 9090
      nodePort: 32171
  selector:
    app: console
